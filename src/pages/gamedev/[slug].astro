---
import { getCollection } from 'astro:content';
import GameDevDetailView from '../../views/GameDevDetailView.astro';

export async function getStaticPaths() {
  const projects = await getCollection('gamedev');
  const enProjects = projects.filter((p) => p.slug.startsWith('en/'));
  return enProjects.map((project) => ({
    params: { slug: project.slug.replace(/^en\//, '') },
    props: { project },
  }));
}

const { project } = Astro.props;
---

<GameDevDetailView project={project} locale="en" />
